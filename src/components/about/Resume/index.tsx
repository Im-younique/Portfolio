// styles
import classes from "./resume.module.scss";

export default function Resume() {
  return (
    <div className={`content-container ${classes.resume_container}`}>
      <p>Resume</p>
    </div>
  );
}
